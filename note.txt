##代码生成##
#业务表配置#
在操作一对多结构的子表时，当前表外键选择子表上与父表的关联列
在操作一对多结构的子表时，子表上与父表关联列的Java类型选择Custom，Java属性名称填父表类名(注意首字母小写)
查询匹配方式选择 Between 时，可在entity上增加辅助属性，以便用于范围性查询
#生成方案配置#
存在一对多结构的多张表，只需要生成父表的代码即可。此时子表没有service、web和view三层，如需这三层的代码，请从子表开始生成，最后到主表

##框架日志##
#删减日志#

##框架开放端口##
#修改配置#
